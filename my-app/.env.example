# Copy this file to `.env.local` and fill in the values.
# Vite exposes any variable prefixed with VITE_ via import.meta.env.

# Public Mapbox access token used by the Hurricane map.
# Create one at https://account.mapbox.com/ and restrict it by URL.
VITE_MAPBOX_TOKEN=
